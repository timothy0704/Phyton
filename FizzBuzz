#FizzBuzz
for num in range(1, 51):
    # Start with an empty result
    output = ""

    # If the number is divisible by 3, add "Fizz" to the result
    if num % 3 == 0:
        output += "Fizz"

    # If the number is divisible by 5, add "Buzz" to the result
    if num % 5 == 0:
        output += "Buzz"

    # If there's no "Fizz" or "Buzz", just print the number
    print(output or num)

